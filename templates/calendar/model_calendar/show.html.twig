{% extends 'base.html.twig' %}

{% block title %}ModelCalendar{% endblock %}

{% block body %}
    <h1>ModelCalendar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model_calendar.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ model_calendar.title }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ model_calendar.state }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ model_calendar.path }}</td>
            </tr>
            <tr>
                <th>Uuid</th>
                <td>{{ model_calendar.uuid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_calendar_model_calendar_index') }}">back to list</a>

    <a href="{{ path('app_calendar_model_calendar_edit', {'id': model_calendar.id}) }}">edit</a>

    {{ include('calendar/model_calendar/_delete_form.html.twig') }}
{% endblock %}
